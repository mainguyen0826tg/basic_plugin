// (C) 2021 GoodData Corporation
import React from "react";
import { serializeObjRef } from "@gooddata/sdk-model";
import { GranteeItemComponent } from "./GranteeItem";
import { GranteeListEmpty } from "./GranteeListEmpty";
/**
 * @internal
 */
export var GranteeList = function (props) {
    var grantees = props.grantees, mode = props.mode, onDelete = props.onDelete;
    if (grantees.length === 0) {
        return React.createElement(GranteeListEmpty, null);
    }
    return (React.createElement("div", null, grantees.map(function (grantee) {
        return (React.createElement(GranteeItemComponent, { key: serializeObjRef(grantee.id), grantee: grantee, mode: mode, onDelete: onDelete }));
    })));
};
//# sourceMappingURL=GranteeList.js.map