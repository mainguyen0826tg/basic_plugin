import { IWidget } from "@gooddata/sdk-backend-spi";
import { IFilter } from "@gooddata/sdk-model";
import { GoodDataSdkError } from "@gooddata/sdk-ui";
import { QueryProcessingStatus } from "../../../model";
/**
 * Hook for obtaining the effective filters for a widget.
 *
 * @param widget - widget to get effective filters for
 * @param filters - additional filters to apply
 * @returns set of filters that should be used to execute the given widget
 *
 * @alpha
 */
export declare const useWidgetFilters: (widget: IWidget | undefined, filters?: IFilter[] | undefined) => {
    result?: IFilter[] | undefined;
    status?: "running" | "error" | "success" | "rejected" | undefined;
    error?: GoodDataSdkError | undefined;
};
//# sourceMappingURL=useWidgetFilters.d.ts.map