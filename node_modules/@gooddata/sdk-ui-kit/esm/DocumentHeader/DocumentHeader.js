// (C) 2007-2020 GoodData Corporation
import React from "react";
import DocumentTitle from "react-document-title";
import AppleTouchIcon from "./AppleTouchIcon";
import Favicon from "./Favicon";
/**
 * @internal
 */
var DocumentHeader = function (props) {
    var _a = props.pageTitle, pageTitle = _a === void 0 ? "" : _a, _b = props.brandTitle, brandTitle = _b === void 0 ? "" : _b, _c = props.appleTouchIconUrl, appleTouchIconUrl = _c === void 0 ? "" : _c, _d = props.faviconUrl, faviconUrl = _d === void 0 ? "" : _d;
    var title;
    if (brandTitle) {
        if (pageTitle) {
            title = pageTitle + " - " + brandTitle;
        }
        else {
            title = brandTitle;
        }
    }
    else {
        title = pageTitle;
    }
    return (React.createElement("div", null,
        React.createElement(DocumentTitle, { title: title }),
        React.createElement(AppleTouchIcon, { url: appleTouchIconUrl }),
        React.createElement(Favicon, { url: faviconUrl })));
};
export default DocumentHeader;
//# sourceMappingURL=DocumentHeader.js.map