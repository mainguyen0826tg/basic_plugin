import { uriRef } from "@gooddata/sdk-model";
/**
 * To perserve the typing and bootstrap concept, we are using firstName
 * as a container for full name and lastname will be an empty string
 */
export var convertUser = function (user) {
    var name = user.name, userId = user.userId, links = user.links;
    return {
        ref: uriRef(links.user),
        login: userId,
        fullName: name,
    };
};
//# sourceMappingURL=UsersConverter.js.map