// (C) 2020 GoodData Corporation
import React from "react";
import { FormattedHTMLMessage } from "react-intl";
var getPreviewTemplate = function (operator, attribute) {
    switch (operator) {
        case "TOP":
            return attribute
                ? "rankingFilter.preview.top_with_attribute"
                : "rankingFilter.preview.top_without_attribute";
        case "BOTTOM":
            return attribute
                ? "rankingFilter.preview.bottom_with_attribute"
                : "rankingFilter.preview.bottom_without_attribute";
        default:
            throw new Error("Operator '" + operator + "' is not supported!");
    }
};
export var Preview = function (_a) {
    var operator = _a.operator, value = _a.value, measure = _a.measure, attribute = _a.attribute;
    return (React.createElement("div", { className: "gd-rf-preview s-rf-preview" },
        React.createElement(FormattedHTMLMessage, { id: getPreviewTemplate(operator, attribute), values: {
                measure: measure.title,
                attribute: attribute === null || attribute === void 0 ? void 0 : attribute.title,
                operator: operator,
                value: value,
            } })));
};
//# sourceMappingURL=Preview.js.map