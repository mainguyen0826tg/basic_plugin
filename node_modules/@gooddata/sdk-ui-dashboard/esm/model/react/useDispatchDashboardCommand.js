import { __read, __spread } from "tslib";
// (C) 2020-2021 GoodData Corporation
import { useCallback } from "react";
import { useDashboardDispatch } from "./DashboardStoreProvider";
/**
 * Hook that takes command creator and returns function that will result into dispatching this command.

 * @param commandCreator - command factory
 * @returns callback that dispatches the command
 * @alpha
 */
export var useDispatchDashboardCommand = function (commandCreator) {
    var dispatch = useDashboardDispatch();
    var run = useCallback(function () {
        var args = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            args[_i] = arguments[_i];
        }
        var command = commandCreator.apply(void 0, __spread(args));
        dispatch(command);
    }, [commandCreator]);
    return run;
};
//# sourceMappingURL=useDispatchDashboardCommand.js.map